[tox]
envlist = py37, lint
skip_missing_interpreters = True
skipsdist = True

[testenv]
deps = pipenv
whitelist_externals = sh

[testenv:lint]
commands =
         pipenv run flake8 automatic_journal
         pipenv run mypy automatic_journal --ignore-missing-imports
         pipenv run isort -c -rc automatic_journal
